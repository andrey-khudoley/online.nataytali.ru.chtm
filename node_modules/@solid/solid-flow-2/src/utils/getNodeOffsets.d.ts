import { NodeData, NodeOffsets } from "../FlowChart";
export declare function getNodeOffsets(nodeIndex: number, offsets: NodeOffsets, nodesData: NodeData[], nodesPositions: Position[], scale: number): {
    offsets: NodeOffsets;
    siblingsIndexes: number[];
};
