import { jsx } from '@app/solid-js';
import { AppUiButton } from './types';
export declare const Button: (props: ButtonProps) => jsx.JSX.Element;
interface ButtonProps {
    block: AppUiButton;
}
export {};
