import { jsx } from '@app/solid-js';
export declare const TabBar: (props: TabBarProps) => jsx.JSX.Element;
interface TabBarProps {
    value: Tab['value'];
    tabs: Tab[];
    onInput?: (value: Tab['value']) => void;
    onClick?: (tab: Tab) => void;
}
export interface Tab {
    value: string | number;
    title: string;
    disabled?: boolean;
}
export {};
