import { jsx } from '@app/solid-js';
import { Condition, Expressions } from './types';
import { Expression } from '/plugin/application/components/expression/types';
export declare const ExpressionsBuilder: (props: ExpressionsBuilderProps) => jsx.JSX.Element;
interface ExpressionsBuilderProps {
    expressions: Expressions;
    readonly?: boolean;
    onAddExpression?(callback: (expression: Expression) => void): void;
    onInput?(expressions: Expressions): void;
    onConditionClick?(condition: Condition, callback: (condition: Condition) => void): void;
    onRemove?(expressions: Expressions): void;
}
export {};
