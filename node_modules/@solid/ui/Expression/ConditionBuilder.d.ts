import { jsx } from '@app/solid-js';
import { Condition } from './types';
export declare const ConditionBuilder: (props: ConditionBuilderProps) => jsx.JSX.Element;
interface ConditionBuilderProps {
    condition: Condition;
    readonly?: boolean;
    onClick?(condition: Condition, callback: (condition: Condition) => void): void;
    onWrap?(condition: Condition): void;
    onRemove?(condition: Condition): void;
}
export {};
