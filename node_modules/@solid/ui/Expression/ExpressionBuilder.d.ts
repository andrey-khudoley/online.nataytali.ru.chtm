import { jsx } from '@app/solid-js';
import { Condition, Expression } from './types';
import { TypedExpressionLeft } from './types';
export declare const ExpressionBuilder: (props: ExpressionBuilderProps) => jsx.JSX.Element;
interface ExpressionBuilderProps {
    expression: Expression | null;
    fields?: TypedExpressionLeft[];
    readonly?: boolean;
    rootConditionTitle?: string;
    onAddExpression?(callback: (expression: Expression) => void): void;
    onInput?(expression: Expression | null): void;
    onConditionClick?(condition: Condition, callback: (condition: Condition) => void): void;
}
export {};
