/**
 * Performs direct call to the special app.function (non-http) internal endpoint of the plugin app
 *  installed in the same account on which the current request is running.
 * This call preserves authentication and environment of the original request.
 */
export declare function runAppFunction<P, R>(ctx: RichUgcCtx, targetApp: InternalCallTarget, path: string): Promise<R>
export declare function runAppFunction<P, R>(
  ctx: RichUgcCtx,
  targetApp: InternalCallTarget,
  path: string,
  params: P,
): Promise<R>
/** @deprecated Use `runAppFunction` instead */
export declare function runInterAppCall<P, R>(ctx: RichUgcCtx, targetApp: InternalCallTarget, path: string): Promise<R>
/** @deprecated Use `runAppFunction` instead */
export declare function runInterAppCall<P, R>(
  ctx: RichUgcCtx,
  targetApp: InternalCallTarget,
  path: string,
  params: P,
): Promise<R>
/**
 * Performs direct call to the special app.function (non-http) endpoint of the account
 *  on which the current request is running.
 * Allows implementing kinda authenticated internal web-hooks called from a plugin app
 *  back to the account code on which the plugin is installed.
 * This call preserves authentication and environment of the original request.
 */
export declare function runAppFunctionInCurrentAccount<P, R>(ctx: RichUgcCtx, path: string): Promise<R>
export declare function runAppFunctionInCurrentAccount<P, R>(ctx: RichUgcCtx, path: string, params: P): Promise<R>
/** @deprecated Use `runAppFunctionInCurrentAccount` instead */
export declare function runInterAppCallToCurrentAccount<P, R>(ctx: RichUgcCtx, path: string): Promise<R>
/** @deprecated Use `runAppFunctionInCurrentAccount` instead */
export declare function runInterAppCallToCurrentAccount<P, R>(ctx: RichUgcCtx, path: string, params: P): Promise<R>
/**
 * High-level "address" of internal call (app.function call or others)
 * Target app slug or 'CurrentAccount' if the target is the current account's code
 */
export declare type InternalCallTarget = string | typeof InternalCallTargetCurrentAccount
export declare const InternalCallTargetCurrentAccount = 'CurrentAccount'
/** @deprecated Use `AppFunctionTargetCurrentAccount` instead */
export declare const InterAppCallCurrentAccount = 'CurrentAccount'

export {}
